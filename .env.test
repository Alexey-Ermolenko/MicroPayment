# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# DB inherits DATABASE_URL from .env; the "_test" suffix (config/packages/doctrine.yaml) targets app_test.
# Redis and Kafka are unused in tests (array cache + in-memory messenger).